"""subset sum problemParent code: knapsackThe problem states that whether a array is subset sum is equal to given sumOp: it should be yes or no.Subset sum+print out the subset using bit manipulationEqual"""def subset_sum(set_array, sum, n):    if sum == 0:        return True    if n == 0:        return False    if sum < set_array[n - 1]:        return subset_sum(set_array, sum, n - 1)    else:        return subset_sum(set_array, sum, n - 1) or subset_sum(set_array, sum - set_array[n - 1], n - 1)array_input = [1, 2, 3, 5]print(subset_sum(array_input, , len(array_input)))